# 待办与实施交接

更新日期：2026-09-22。用户已确认最终预览，由后续 **5.6 sol** 实施 UI；当前阶段只提交计划和排查客户端问题，不直接修改正式 UI。

## 已确认的依据

- [产品计划第 3.1 节](plan.md#31-布局与操作)是新交互要求，优先于旧版一次展示所有题目的实现。
- 最终预览 commit：`2fe1f83`；[独立预览](docs/previews/chat-flow.html)、[多题分页](docs/previews/chat-flow-multiple.png)、[统一摘要](docs/previews/chat-flow-multiple-submitted.png)。
- 预览服务：`http://localhost:8790/chat-flow.html`，启动命令见[预览说明](docs/previews/README.md)。
- 用户目标：B 版选中高亮；提问紧贴输入框且不随聊天滚动；一次一题，右上角 `1 / N` 前后导航；最后提交后保留一张完整问答摘要。

## 阶段 1：先核验宿主能力

- [ ] 查阅最新 OpenAI/MCP Apps 官方接口，记录内嵌 UI 定位、显示模式、关闭和尺寸通知能力。
- [ ] 在真实 ChatGPT 中验证是否存在受支持的“固定到宿主输入框上方”路径，记录环境与证据。
- [ ] 若该路径不可用，列明能力缺口和可审阅的替代呈现方案，与用户对齐；不静默降级或使用跨域 DOM 注入。
- [ ] 区分独立预览与正式 iframe：不把预览聊天输入栏、模拟助手回复、模式切换工具栏复制进产品。

## 阶段 2：B 版视觉、点击与分页

- [ ] 在 `ui/src/App.tsx`、`components/QuestionCard.tsx` 和 `styles.css` 实现 B 版样式与整行选择；消除点击留白无效的区域。
- [ ] 普通项选中后展开详情；description 与用户输入分离，保留切换后的草稿。
- [ ] 单题直接提交，多题每次显示一题、右上角页数和前后按钮；返回时恢复各题全部草稿。
- [ ] 前进校验当前题，最后校验全表；可选题可留空，必填/Other 错误定位至对应题目。
- [ ] 保留单选、多选、文本、确认及 `allow_other`，不增加排序/拖拽题或未确认的 Skip/关闭语义。

## 阶段 3：键盘、发送与收起

- [ ] 用统一提交路径处理按钮与 Enter；前面的页按 Enter 下一题，单题/最后一页按 Enter 提交。
- [ ] Shift + Enter 换行；覆盖中文 IME 组合输入与确认选字，不能误提交或误翻页。
- [ ] 发送中锁定编辑、翻页及重复提交；成功后仅展示完整问答摘要，去除所有编辑和分页控件。
- [ ] 失败保留状态，可手动重试/复制 Q/A；未确认成功不能收起，不能自动重发。
- [ ] 复用现有答案格式化规则，确保摘要与 `ui/message` 内容一致；不添加问卷 ID、数据库或跨卡片同步。
- [ ] 保持界面本地化、暗色模式、可访问名称、焦点提示与小屏幕可操作性。

## 阶段 4：验证与交付

- [ ] 增补必要的真实行为测试：整行点击、输入不切换选项、分页草稿、必填/可选题、IME、Enter、重复提交、失败重试和成功摘要。
- [ ] 使用仓库实际命令 `npm run check`；依赖需要安装时使用 `npm install`。
- [ ] 在目标浏览器及可用客户端执行真实 MCP 展示/答案回传验证，分别记录调用失败、UI 不显示和发送失败。
- [ ] 按 `plan.md` 第 12 节的 18–21 项更新 `docs/acceptance.md`；不将本地模拟视为宿主能力验证。
- [ ] 同步 README、计划及相关验收项，完成每个阶段检查后仅提交相关文件；默认不推送、不发布。

## 当前独立排查：原生客户端调用失败

排查记录见 [客户端排查](docs/client-troubleshooting.md)，更新于 2026-09-23。

- [x] 验证本地健康/就绪、随机端口 doctor、组织关联数量及携带/不携带组织头的元数据读取；只读检查通过，未改变生产配置。

- [ ] 手机/iPad 返回缺少 active organization context：核对客户端版本、账号/工作区、Tunnel 关联和失败阶段。
- [ ] Windows 原生客户端静默失败：区分未调用、Tunnel 拒绝和 UI 渲染失败，不能只根据截图认定根因。
- [ ] 对比正常的移动/PC 浏览器与失败客户端，记录请求时间和脱敏证据；本地 Tunnel 健康不等于所有客户端可用。
